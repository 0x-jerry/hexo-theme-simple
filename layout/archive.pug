-
  let title = __('archives.title')
  if (page.tag) title = page.tag.toString()


.simple-archives
  if title
    h1.simple-archive-title=title
      small
        | (#{page.posts.data.length})

  each item in page.posts.data
    .simple-article-archive
      h2.title
        a(href=config.root+item.path)
          = item.title || item.link
      p.meta
        != item.date.format(config.date_format)
        if item.author
          | by
          = item.author
  include _partial/pagination
