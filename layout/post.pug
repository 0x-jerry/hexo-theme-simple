.simple-artical
  h2.title
    = page.title || page.link
  p.meta
    = page.date.format(config.date_format)
    if page.author
      | by
      = page.author
  #post-content
    != page.content
  br
  include _partial/article-tags
  include _partial/article-categories
